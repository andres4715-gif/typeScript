class Button {
  start: boolean;
  on: boolean;
  off: boolean;

  constructor(start: boolean, on: boolean, off: boolean) {
    this.start = start;
    this.on = on;
    this.off = off;
  }
}

export default Button;
